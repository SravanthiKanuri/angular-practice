import { FormateCurrencyPipe } from './formate-currency.pipe';

describe('FormateCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
